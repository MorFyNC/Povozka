@page "/chats"
@rendermode InteractiveServer

<PageTitle>Chats</PageTitle>

<style>
    .chat-container {
      max-width: 600px;
      margin: auto;
    }
    .message {
      border-radius: 10px;
      padding: 10px 15px;
      margin-bottom: 10px;
    }
    .message.sender {
      background-color: #DCF8C6;
      align-self: flex-end;
    }
    .message.receiver {
      background-color: #E5E5EA;
      align-self: flex-start;
    }
  </style>
  <div class="container chat-container">
    <div class="row">
      <div class="col">
        <div class="message receiver">
          <p>Привет, как дела?</p>
        </div>
        <div class="message sender">
          <p>Привет! Всё отлично, спасибо!</p>
        </div>
        <div class="message receiver">
          <p>Чем занимаешься?</p>
        </div>
        <div class="message sender">
          <p>Просто кодирую немного. А у тебя?</p>
        </div>
        <!-- Продолжайте добавлять сообщения по аналогии -->
      </div>
    </div>
    <div class="row">
      <div class="col">
        <div class="input-group mb-3">
          <input type="text" class="form-control" placeholder="Напишите сообщение..." aria-label="Напишите сообщение..." aria-describedby="button-send">
          <button class="btn btn-outline-secondary" type="button" id="button-send">Отправить</button>
        </div>
      </div>
    </div>
  </div>

@code {

}
